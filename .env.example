# Copy to .env and fill in. Never commit .env.
UPSTOX_API_KEY=
UPSTOX_API_SECRET=
UPSTOX_REDIRECT_URI=http://localhost:8721/callback
# Optional: paste a token directly instead of running `python main.py auth`
UPSTOX_ACCESS_TOKEN=
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
# Must be exactly "yes" before any real order is placed (Phase 6)
CONFIRM_LIVE_TRADING=
